#include <iostream>
#include "header.h"

int main() {
    std::cout << "#include <standard library> : used for including Standard Library headers." << std::endl;

    std::cout << "std:: prefix is used in C++ to refer to functions, classes, and objects that belong to the Standard Library." << std::endl; 

    std::cout << "#include \"header.h\" :  used for including Local header files in the same directory of the .cpp file." << std::endl;
}